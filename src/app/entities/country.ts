import { Time } from '@angular/common';

export class Country {
    constructor(public id: string, public name: string, public capital: string, public region: string, public population: number) {}
}
